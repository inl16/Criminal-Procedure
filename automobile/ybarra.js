function isSearchOfPersonConstitutional(place, person){
    //check if the officre have proablb cuase to search 
    if(!probableCause){
        return false;
    }

    //check if the perosn has a connectoin to the place being search 
    if(!person.hasConnectionToPlace){
        return false;
    }

    //the serach is constitutonal
}